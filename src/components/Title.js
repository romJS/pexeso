import React from "react";
import styled from "styled-components";

const StyledTitle = styled.h1`
  color: #000;
  font-size: 60px;
  position: relative;
`;

const Title = props => {
  return <StyledTitle>PEXESO</StyledTitle>;
};

export default Title;
